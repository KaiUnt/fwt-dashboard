# Production dependencies - Render.com compatible
fastapi==0.104.1
uvicorn[standard]==0.24.0
aiohttp==3.9.1
python-multipart==0.0.6
httpx==0.25.2
python-dotenv==1.0.0
slowapi==0.1.9

# Use older pydantic version to avoid Rust compilation issues on Render.com
pydantic==2.4.2

# Testing dependencies (only for development)
pytest==7.4.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0 